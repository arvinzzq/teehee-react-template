{
  "name": "{{ name }}",
  "description": "{{ description }}",
  "version": "{{ version }}",
  "author": "{{ author }}",
  "license": "{{ license }}",
  "scripts": {},
  "keywords": [
  {{#each keywords}}
    "{{this}}"{{#if @last}}{{else}},{{/if}}
  {{/each}}
  ]
}
